import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.raajpatel.app',
  appName: 'TimelyTravel',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
